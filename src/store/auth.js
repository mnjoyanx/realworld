export default {
    state: {
        isSubmitting: false
    },
    mutations: {},
    actions: {},
    getters: {
        submitForm(state) {
            return state.isSubmitting
        }
    },
}